VIRTUAL ENVIRONMENT SETUP✔️
requirements.txt file created✔️

5.create notebooks clean and perform eda on old data

6. load clean data and train model
#BASE_DIR = os.path.abspath(os.path.join(os.getcwd(), ".."))  
data_path = os.path.join(BASE_DIR, "data", "cleaned_quikr_car.csv")
data.to_csv(data_path, index=False)

7. Save model in artifacts dir

#with open("artifacts/model.pkl", "wb") as f:
    pickle.dump(model, f)
print("Model trained and saved successfully!")

8.create requirements.txt file and add dependencies there 
  create .gitignore file and add venv/ mlruns/ __pycache__/ artifacts/ .dockerignore to it (to ignore these files in git)

9. first push to github (after model saving) 
>git add .
>git commit -m "Initial commit - model training and saving"
>git remote add origin https://github.com/yourusername/mlops-complete-project.git
>git push origin main
(to rest branch name from master to main : > git branch -M main)

10.Build API (using FastAPI)

11.Load model and Create /predict endpoint and Test locally
http://127.0.0.1:8000/docs a UI will open where you can test the API
Status code 200 means API is working fine

